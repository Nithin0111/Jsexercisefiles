//starting with basic
var score = 100;
score = 130;
const bonus = 50;
var totalScore = ((score + bonus) * 1.5)/2;//operator precedence
console.log(totalScore);
//program to convert farenheit to celsius in js
var far = 72;
var celsius = ((far - 32) * 5/9);
console.log("temperature in celsius is: " ,celsius);
